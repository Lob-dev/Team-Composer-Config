spring:
  application:
    name: eureka-service

eureka:
  instance:
    hostname: localhost

  client:
    serviceUrl:
      defaultZone: http://localhost:9001/eureka/
    register-with-eureka: false
    fetch-registry: false

server:
  port: 9001